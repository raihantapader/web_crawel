services:
  # Run all examples (default)
  crawler:
    build: .
    volumes:
      - ./output:/app/output

  # Run tests
  test:
    build: .
    command: pytest tests/ -v --cov=src/crawler --cov-report=term-missing
    profiles:
      - test
